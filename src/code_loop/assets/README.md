# Bundled browser assets

`mermaid.min.js` is the pinned Mermaid 11.16.0 browser distribution. Its MIT
license is preserved in `MERMAID_LICENSE`. The asset is copied once into each
repository's Code Loop output directory so reports remain fully local.
