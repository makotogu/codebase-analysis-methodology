# 02 证据体系：代码事实与业务语义双轴

AI 分析代码库的可信度几乎完全取决于证据管理。本篇定义两个正交的证据轴，以及围绕它们的两个长期机制：术语对照表和错位探测器。

## 一、为什么是两个轴

AI 能验证的是**代码事实**："这个函数在条件 B 下会写表 A"——它可以打开文件、追调用、跑代码来确认。但它永远无法验证**业务语义**："这就是财务口中的'结算'"。存量系统里命名漂移、历史包袱、口头约定俯拾皆是，源码的命名和逻辑可能就和业务实际内容对不齐。AI 再强也只能顺着命名推断，而命名本身可能是错的。

所以证据体系必须拆成两个正交的轴，分别推进：

**代码轴（AI 可自主推进）：**

| 等级 | 含义 |
|---|---|
| `verified` | 由本地文件或命令确认，附 file:line 或命令输出 |
| `inferred` | 从命名、目录结构、惯例推断，尚无直接证据 |
| `unverified` | 未验证，需要构建/运行/源码追踪 |

**业务轴（只能由人推进）：**

| 等级 | 含义 |
|---|---|
| `business-confirmed` | 由具名的人确认业务含义，记录来源，如"据张三，2026-07" |
| （缺省） | 业务含义未经人确认，即使代码轴已 verified |

规则：

- 没有文件路径支撑的架构描述一律视为假设，禁止进入结论。
- 代码轴 verified 不代表业务轴可信：AI 可以确认"函数 X 写了表 A"，但"表 A 是不是业务上的结算流水"必须由人确认。
- `business-confirmed` 必须具名、带日期。业务理解会过时，来源和时间让它可审计、可更新。
- 地图、追踪记录、重构提案里的每个关键论断都带证据标签；重构决策只允许建立在 verified + business-confirmed 的论断之上，其余论断先补证据。

## 二、术语对照表（glossary）：复利的长期资产

一张"代码命名 ↔ 业务真实含义 ↔ 负责人"的映射表，由人维护，作为上下文注入每一次 AI 会话。它直接对冲命名对不齐的问题，而且是**复利的**：每澄清一个术语，之后所有分析都受益。

建议的结构（`glossary.yaml`）：

```yaml
- code_term: SettleService        # 代码里的命名
  business_meaning: 实际处理的是对账，不是结算；真正的结算在 ClearingJob
  owner: 张三
  confirmed_by: 张三
  confirmed_date: 2026-07
  evidence: src/main/java/.../SettleService.java
```

使用纪律：

- glossary 是人工段，机器重跑任何分析都不得覆盖它（机器段/人工段分离原则，见 [04-工具箱设计.md](04-工具箱设计.md)）；
- 每次 Agent 会话开始时注入 glossary，让 AI 在已澄清的术语上不再犯错；
- 访谈产出的每个澄清都回填 glossary，而不是散落在聊天记录里。

## 三、AI 的角色反转：从"解释者"到"错位探测器"

不要问 AI"这段代码的业务含义是什么"——它只会顺着命名编。把角色反转：让 AI **标记疑似错位**，产出的不是结论，而是一份**给领域专家的访谈问题清单**。

错位探测器要找的模式：

- 命名和实际行为矛盾的函数（名为 query 却有写操作、名为 validate 却顺手改了状态）；
- 注释和代码打架的地方（注释说的分支条件和实际条件不一致）;
- 名为 X 却顺手干了 Y 的方法（职责溢出）；
- 同一个业务词在不同模块里指不同东西（术语冲突候选）。

工作流是一个闭环：

```mermaid
flowchart LR
    hotModules["热点模块<br/>(只跑热点,控制成本)"] --> detector["AI 错位探测"]
    detector --> questions["访谈问题清单"]
    questions --> interview["人工访谈领域专家"]
    interview --> glossaryStore["回填 glossary"]
    glossaryStore -.注入后续所有会话.-> detector
```

这样人工的介入是**聚焦的、高杠杆的**：人不需要逐行陪读，只需要回答 AI 筛出来的高价值问题；每个回答又让后续的 AI 分析更准。

## 四、追踪记录的证据分级

对运行时行为的论断，按获取方式分级（从高到低）：

1. **真实调试追踪**：断点、栈帧、变量、输出来自运行中的进程；
2. **日志插桩追踪**：运行时证据来自新增或既有日志、命令输出；
3. **源码推演追踪**：调用链仅从本地源码推断——每个运行时论断必须标 unverified；
4. **部分追踪**：至少一段已验证，但完整旅程被阻塞。

纪律：**永远不要把预期输出当成真实输出写进记录**。跑不起来就标"未运行"，推演就标"推演"。一份诚实标注的低等级追踪，价值远高于一份看似完整的编造记录。
