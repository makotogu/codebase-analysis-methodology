# 03 重构决策：从独立信号到可回退执行

本篇适用于 Phase 3–5。输入是覆盖账本、技术/业务证据、Git/故障/工单/运行数据和指标基线；产物是候选评估、需求信号、重构提案、readiness 与执行观测。

## 一、代理指标用于发现候选，不是决策真值

| 信号 | 原始数据 | 正确解读 |
|---|---|---|
| change-frequency hotspot signal | 窗口内变更次数 × LOC/复杂度 | 频繁且修改成本可能较高 |
| `change-coupling signal` | 同一变更单元内共同修改 | 存在共同变化模式，原因待验证 |
| `error-signal density` | 去重错误簇 / 声明规模 | 日志错误信号集中，不等于缺陷密度 |
| `ownership concentration proxy` | 指定窗口作者贡献分布 | 知识可能集中，不等于真实 bus factor |
| `reachability-weighted exposure estimate` | 入口观测权重沿静态图传播 | 调查暴露度估计，不是真实模块流量 |

所有信号保留原始分子、可用分母、归一化、窗口、过滤和不确定性。候选至少由两个**独立数据生成机制**支持，例如 Git 变更 + 工单回归，不能用同一份 Git 数据衍生的两个分数凑数。

## 二、变更轴线和需求侧事实

共同变化可能来自同一业务概念，也可能来自批量格式化、版本升级、机械迁移或团队提交习惯。过滤巨型 commit、追踪 rename，并抽样阅读提交/工单后，才可提出“概念被切碎”或“模块承担多轴线”的候选。

同时维护 `demand-signals.yaml`：历史需求、路线图、监管约束和能力归属。每条记录包含来源、时间范围、负责人、业务状态和不确定性。过去频繁变更不代表未来仍值得投入；未来路线图也不能替代技术问题证据。

## 三、三道立项门槛

1. **技术问题**：关键 Claim 有适配证据，并有至少两个独立信号；
2. **业务与需求**：涉及业务语义时为 `business-confirmed`，且存在历史或未来需求信号；
3. **可测收益**：已有基线、目标值和观察窗口，明确停止或复盘条件。

任一门槛缺失，提案退回补证。通过表示“值得立项”，不表示“可以生产迁移”。模板见 [refactor-proposal.md](templates/refactor-proposal.md)。

## 四、执行 readiness

生产迁移前逐项确认：

- 负责人和干系人；
- 表征测试或其他行为 Oracle；
- 可观测性和成功指标；
- 发布、灰度和回滚；
- 数据迁移与兼容；
- 安全、隐私和合规；
- 停止条件。

readiness 未通过时，可批准 spike、表征测试和补观测，不得进入生产迁移。

## 五、小步执行与治理

优先使用可独立回退的步骤：先锁定行为，再让新实现在旁路生长，按可观测信号逐步切换。表征测试只记录已覆盖行为，不自动证明业务正确；生成测试必须人工审查 Oracle 和边界条件。

目标边界可写成 CI 适应度断言。存量违规先登记 baseline，新增违规阻断，存量按计划递减。棘轮不能取代发布观察和负责人裁决。

## 六、阶段门与维护

Phase 3 的退出条件是指标口径和独立性可审计；Phase 4 的退出条件是三道门槛和裁决记录完整；Phase 5 的进入条件是 readiness 通过。执行后按基线、目标和窗口复测，达不到目标或触发停止条件就暂停/回退。

候选由分析负责人维护，需求信号由产品/领域负责人复查，执行证据由交付负责人维护。数据窗口结束、业务路线变化、基线口径变化或目标模块大幅改动时，评估过期。
