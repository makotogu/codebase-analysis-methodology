# 09 多仓库联合分析：声明分母与跨仓验证

本篇适用于前端、后端、BFF、库和基础设施共同构成的 workspace。输入是人工声明的 `repos.yaml`、各仓事实源、网关/契约/注册与发布数据；产物是跨仓候选边、置信度、差异账本和验证任务。

## 一、`repos.yaml` 是声明分母

`repos.yaml` 只代表维护人声明纳入的仓库，不天然覆盖组织全部代码、部署或第三方系统。记录声明人、日期、已知排除项和复查日；覆盖率只在这个分母内计算。仓库名作为 stable ID 前缀，移动或拆分使用 alias/lineage。

## 二、跨仓边及其证据

| 边 | 发现方法 | 常见断点 | 默认解释 |
|---|---|---|---|
| RPC | 契约 + version/group join | 动态配置、泛化、跨语言 | `source-verified` 或 `inferred` 候选 |
| HTTP/API | method + 归一化路径模糊 join | 拼接 URL、网关重写、移动端/第三方调用 | 带 exact/prefix/fuzzy/unmatched 的候选 |
| 共享表 | mapper/entity/SQL 解析 | 动态 SQL、存储过程、schema 同义词 | 数据所有权调查信号 |

OpenAPI、网关配置和注册中心可用于对账，但各自仍有环境、版本和时效边界。`unmatched` 只产生扫描缺口、外部调用方或废弃候选，不能直接称死接口。

## 三、模糊 API join

`api-edges.yaml` 保存原始调用点、归一化规则、候选目标、match 类型、证据类型、置信度和验证任务。exact 也只能说明文本/契约匹配；要证明目标环境中实际调用，仍需测试、抓包、网关日志或 trace 形成 observed 证据。

## 四、跨仓 change-coupling signal

工单 ID 聚合优于发布时间近似，但都可能受提交规范、批量发布和组织流程影响。发布时间产生的 `change-coupling signal` 必须保存窗口、时区、发布批次、作者/机器人过滤与样本数，默认低置信。共享表推断也要保留解析覆盖率和动态 SQL 断点。

跨多个服务共同变更可能是契约演进、发布编排或团队习惯，不是“边界错误铁证”。它必须与业务语义、故障/交付或运行观察等独立信号交叉验证。

## 五、工作区投影与维护

各仓库保留本地事实源；工作区层只合并 stable ID、边和摘要，不复制并覆盖子仓人工事实。服务地图、Mermaid 和 HTML 是投影。glossary 可以工作区共享，但条目必须限定适用仓库/产品/区域，并允许 disputed。

进入下一阶段前：Phase 1 需要让声明分母、扫描器、跨仓差异和未知外部系统可审计；Phase 2 对关键边补 observed 证据；Phase 3 才组合独立信号。仓库增删、网关规则、接口版本、发布流程或窗口变化时产物过期，由 workspace 维护人和仓库维护人共同复查。
