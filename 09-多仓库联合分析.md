# 09 多仓库联合分析：前端 × 后端工作区

真实场景很少是单仓库：前端 x 个仓库、后端 y 个仓库，必须同时分析。核心主张：**分析的单位是"工作区"（workspace），不是仓库**——单仓库工具照常在每个仓库内跑，但地图、热力图、co-change 都要有工作区级的合并视图；而合并的关键是识别三类"跨仓库边"，它们的静态可得性完全不同。

## 一、工作区清单：分析的分母

第一步永远是建 `repos.yaml`（见 templates/repos.yaml）：每个仓库的名字、路径、kind（frontend / backend / bff / lib）、语言、对应服务名。这张表的作用等同于单仓库的入口清单——**它是完备的分母**，进度、覆盖率、遗漏检查都以它为准。仓库名从此成为所有 stable ID 的前缀（`web-portal::src/api/order.ts`），保证工作区内全局唯一。

## 二、三类跨仓库边：静态可得性决定打法

| 边类型 | 连接 | 静态可得性 | 打法 |
|---|---|---|---|
| RPC 边 | 后端 ↔ 后端 | **高**（强类型接口） | FQCN+version+group 静态 join + 注册中心对账（见 [06-分布式与Dubbo.md](06-分布式与Dubbo.md)） |
| API 边 | 前端 → 后端 | **中**（字符串 URL） | 前端扫 HTTP 调用 + 后端路由表模糊 join，带匹配置信度 |
| 共享表边 | 任意 ↔ 任意 | **高**（mapper/entity 可解析） | T2.5 在所有后端仓库跑，合并成"表 × 服务"读写矩阵 |

### API 边：前端到后端的 join 是模糊的，要显式管理置信度

前端调用后端靠字符串 URL，join 不可能像 Dubbo 那样精确，流程是：

1. **前端侧提取**：扫 axios/fetch/自封装 client 的调用点，提取 method + 路径模板。存量前端工程几乎一定有一层 API 封装模块（`src/api/`、`services/`）——**优先扫这一层**，它就是前端的"出口普查"；散落在组件里的裸调用是普查漏洞候选。
2. **路径归一化**：把模板字符串、路径拼接归一成占位符形式（`/api/v1/orders/${id}` → `/api/v1/orders/{*}`）。
3. **与后端 join**：和所有后端仓库 entries.yaml 的 http 条目按 method + 路径匹配，结果分级：`exact`（完全匹配）/ `prefix`（网关前缀剥离后匹配）/ `fuzzy`（占位符对齐）/ `unmatched`。产出 `api-edges.yaml`（见模板）。
4. **对账**：`unmatched` 的前端调用 → 后端死接口候选或普查漏洞；没有任何前端引用的后端 http 入口 → 死接口候选（还要排除 App、第三方回调等其他调用方）。

两个现成的捷径：**有 OpenAPI/Swagger 就先用它做 join 的基准**（相当于 actuator 对账的前端版）；**网关路由配置**（nginx/网关服务的转发规则）是前缀剥离规则的权威来源，必须纳入解析。**BFF 仓库注意**：它同时有 API 边的两端（对前端是 provider，对后端是 consumer），在 repos.yaml 里标 kind=bff，两侧都要扫。

## 三、跨仓库 co-change：工单 ID 优先，时间窗兜底

T1 的 co-change 靠"同一个 commit"，跨仓库天然失效。两条替代路径（git_miner.py 已实现）：

- **工单 ID（首选）**：commit message 里有需求/工单号规范的（`PAY-123`），按工单聚合跨仓库变更集。信号质量高，直接可用；
- **时间窗近似（兜底）**：没有工单规范时，用"同一作者在 ±N 小时内在多个仓库提交"近似。噪音大，只用于仓库级信号，不下钻到文件级。

解读上多了一层：**前端仓库和某个后端仓库长期强耦合，通常不是坏事**（页面和它的 API 本来就该一起变）；值得警惕的是这两种模式——

- **一个需求总要同时改 2 个以上后端仓库** → 服务边界切错了的铁证（微服务版"被切碎的概念"）；
- **前端仓库 A 总是和"不属于它页面域"的后端仓库耦合** → 接口契约混乱或 BFF 缺位的信号。

## 四、工作区级地图：每仓库一层，再加一张总图

延续"一份数据、两个投影"（[07-面向人与模型的表示.md](07-面向人与模型的表示.md)）：

- 每个仓库照常产出自己的模块卡片和热力图（单仓库工具不变）；
- 工作区级新增一张**服务地图卡片**：节点 = 仓库/服务，边 = 三类跨仓库边（各带证据等级），权重 = 跨仓库 co-change 强度与 API/RPC 调用量；
- stable ID 全部带仓库前缀；glossary 是**工作区级唯一一份**——术语在前后端之间漂移（前端叫"套餐"后端叫"bundle"）恰恰是错位探测的高发区。

## 五、落地顺序（相对单仓库的增量）

1. 建 `repos.yaml`，把所有仓库 clone 到同一工作区目录；
2. T1 在每个仓库跑 + 跨仓库 co-change（`git_miner.py` 多仓库模式，优先配 `--ticket-regex`）；
3. 后端各仓库跑 T2/T2.5，合并出 RPC 边和共享表矩阵（06 篇）；
4. 前端各仓库扫 API 封装层，与后端路由表 join 出 API 边（本篇第二节）；
5. 合成工作区服务地图；`unmatched` 与共享表矩阵进入重构决策的输入。

单仓库方法论的其余部分（证据体系、错位探测、梳理、执行）不变——它们天然是工作区级的。
