# 04 工具箱设计：T1–T8 的数据合同

本篇适用于 Phase 1–5，也定义 Code Loop（Phase 0）到正式工具链的交接。输入是候选问题、仓库声明、分析策略和可用数据；产物必须是可 diff 的 CSV/YAML/Markdown，并引用统一 artifact metadata。

## 一、共同约束

1. parser、AST、git 和运行采集负责确定性部分；LLM 只做归因、摘要、分类候选和错位探测；
2. 每个产物记录工具版本、commit、扫描范围、配置、环境、窗口、采样、负责人和过期条件；
3. 机器段可重跑，人工段不可覆盖；stable ID 支持 `aliases`、`previous_id`、`lineage`；
4. 新产物使用新证据枚举，旧 `verified` 仅兼容解释为 `source-verified`；
5. 原始分子和差异账本保留，综合分不能替代原始数据。

## 二、T1–T8 输出合同

| 工具 | 阶段 | 输入 | 输出与正确名称 | 关键不确定性 |
|---|---|---|---|---|
| T1 git-miner | 3 | git 历史、窗口、过滤规则 | `hotspot.csv`、`change-coupling.csv`、`ownership-concentration.csv` | 巨型 commit、rename、机器人、贡献口径 |
| T2 entry-census | 1 | 仓库声明、扫描器配置 | `entries.yaml` + `coverage-ledger.yaml` | 动态注册、条件配置、外部平台 |
| T3 log-census | 3 | 日志模板、运行日志、窗口 | `error-signal-density.csv` | 漏打、重复打印、采样、归属失败 |
| T4 choke-metrics | 2 | 收口点、分析策略、环境 | `entry-observations.csv` | 隐私、版本、窗口、失败回滚 |
| T5 reachability | 3 | 入口观察 + 静态图 | `reachability-weighted-exposure.csv` | 反射、异步、分支、传播算法 |
| T6 map-builder | 1–3 | 上述事实源、glossary | 模块卡片、机器/人类投影 | 摘要丢失、stale 引用、ID 漂移 |
| T7 mismatch detector | 2–3 | 热点 Claim、glossary | 访谈问题与争议队列 | 业务来源冲突、确认过期 |
| T8 fitness checks | 5 | 已批准边界、baseline | CI 断言与棘轮记录 | 误报、豁免扩散、规则所有权 |

### T1：Git 信号

解析 `git log --numstat`，保留时间衰减、巨型 commit 过滤和 rename lineage。共同变更只称 `change-coupling signal`；作者分布只称 `ownership concentration proxy`。它们用于排序调查，不能自动产生模块边界或人员风险结论。

### T2：入口与覆盖

`entries.yaml` 保存入口 stable ID、类型、file:line 和证据类型。`coverage-ledger.yaml` 保存声明分母、扫描器、运行对账、差异、未知来源与覆盖公式。“运行有/静态无”是扫描缺口候选；“静态有/运行无”不是死代码证明。

### T3–T5：运行信号与估计

T3 保留原始日志条数、去重规则和归属率。T4 仅在获批环境按 `analysis-policy.yaml` 采集，避免密钥/个人数据进入日志或远程模型。T5 输出传播估计，并公开图断点与算法版本，不称“真实运行热度”。

### T6–T8：表示、确认与守护

T6 从数据事实源生成模型投影和人类投影；Mermaid/HTML 是可重建视图，不是事实源。T7 输出问题而不是业务结论。T8 仅在边界获批后守护，并为规则设置 owner、豁免期限和误报处理。

## 三、Phase 0 到正式工具链的交接

Code Loop 输出的 Claim、file:line、候选链路和未决项先进入**导入队列**：

1. 固定 workspace commit，当前 `verified` 按 `source-verified` 解释；
2. 按主问题选择进入入口普查、覆盖差异、运行验证或 glossary 访谈；
3. 重新用相应工具校验引用，不直接覆盖 `entries.yaml`、glossary 或长期地图；
4. 记录来源 session/revision，分配 stable ID 和 artifact metadata；
5. 把运行未知、业务未知和范围扩展分别转成验证任务。

Code Loop 能支持“某 commit 上的局部源码候选”，不能推出入口普查完成、生产路径成立或重构优先级。

## 四、落地顺序与过期

快速调查只需 Code Loop/等价 Phase 0 工具。标准评估先建分析策略与 metadata，再做 T2 覆盖账本、T1/T3 信号、必要的 T4 验证，最后 T5/T6 综合；T7 随访谈推进。T8 只在 Phase 5 使用，棘轮版可在边界已确认时提前止血。

工具版本、commit、配置、声明分母、环境或数据窗口变化会使产物 stale。metadata 中的 owner 负责复查；没有维护人和过期条件的机器产物不得成为长期事实源。
